using UnityEngine;
using System.Collections;

public class ModeChange : Singleton<ModeChange> {

	public enum VRmode { on, off };

	public VRmode VRMode;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
